<Window x:Class="DataVault.Client.Screens.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DataVault.Client.ViewModels"
        Title="Вход в систему" Height="480" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="{StaticResource AppFont}">
    <Window.DataContext><vm:LoginViewModel/></Window.DataContext>
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="32,28">
            <StackPanel MaxWidth="320" HorizontalAlignment="Center">
                <TextBlock Text="DataVault" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                <TextBlock Text="Учёт ресурсов и задач" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,24"/>

                <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="24,20" Margin="0,0,0,18">
                    <StackPanel>
                        <TextBlock Text="Имя пользователя" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,16"/>

                        <TextBlock Text="Пароль" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{StaticResource SurfaceBrush}">
                            <PasswordBox x:Name="PwdBox" PasswordChanged="PwdBox_PasswordChanged"
                                        Style="{StaticResource PasswordBoxStyle}" BorderThickness="0" Background="Transparent"/>
                        </Border>

                        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource AccentBrush}" TextWrapping="Wrap" Margin="0,12,0,0"
                                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" FontSize="11"/>

                        <Button Content="Войти" Click="SignInButton_Click" Style="{StaticResource PrimaryButton}"
                                HorizontalAlignment="Stretch" Margin="0,20,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
