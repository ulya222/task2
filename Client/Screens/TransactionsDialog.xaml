<Window x:Class="DataVault.Client.Screens.TransactionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="История операций" Height="380" Width="580" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}" FontFamily="{StaticResource AppFont}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/DataVault.Client;component/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="TransactionsGrid" Grid.Row="0" Style="{StaticResource ModernDataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ресурс" Binding="{Binding Resource.Name}" Width="*"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding TransactionType}" Width="70"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="60"/>
                <DataGridTextColumn Header="Хранилище" Binding="{Binding Storage.Name}" Width="110"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy HH:mm}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="1" Content="Закрыть" Click="Close_Click" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Right" Margin="0,14,0,0"/>
    </Grid>
</Window>
