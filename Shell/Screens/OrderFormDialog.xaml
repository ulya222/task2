<Window x:Class="TelecomProd.Shell.Screens.OrderFormDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новый производственный заказ" Height="380" Width="480" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}" FontFamily="{StaticResource AppFont}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TelecomProd.Shell;component/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="DatePickerStyle" TargetType="DatePicker">
                <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,8"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Text="Узел сборки" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
            <ComboBox x:Name="UnitCombo" DisplayMemberPath="Name" SelectedValuePath="Id" Style="{StaticResource ComboBoxStyle}" Margin="0,0,0,18"/>
            <TextBlock Text="Количество" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
            <TextBox x:Name="QtyBox" Text="1" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,14"/>
            <TextBlock Text="Плановая дата завершения" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
            <DatePicker x:Name="PlanDatePicker" Style="{StaticResource DatePickerStyle}" Margin="0,0,0,18"/>
        </StackPanel>
        <Border Grid.Row="1" Padding="0,18,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Создать" Click="Apply_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,14,0"/>
                <Button Content="Отмена" Click="Cancel_Click" Style="{StaticResource SecondaryButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
