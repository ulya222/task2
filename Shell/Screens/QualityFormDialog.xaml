<Window x:Class="TelecomProd.Shell.Screens.QualityFormDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Результат тестирования" Height="380" Width="460" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}" FontFamily="{StaticResource AppFont}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TelecomProd.Shell;component/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Text="Процедура тестирования" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
            <TextBox x:Name="ProcedureBox" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,14"/>
            <TextBlock Text="Результат измерений" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
            <TextBox x:Name="ResultBox" Style="{StaticResource TextBoxStyle}" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="60" Margin="0,0,0,14"/>
            <CheckBox x:Name="PassedBox" Content="Тест пройден" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,14"/>
            <TextBlock Text="№ сертификата (опц.)" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
            <TextBox x:Name="CertBox" Style="{StaticResource TextBoxStyle}"/>
        </StackPanel>
        <Border Grid.Row="1" Padding="0,18,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Сохранить" Click="Apply_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,14,0"/>
                <Button Content="Отмена" Click="Cancel_Click" Style="{StaticResource SecondaryButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
