<Window x:Class="TelecomProd.Shell.Screens.BomConstructorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Конструктор спецификации (BOM)" Height="520" Width="800" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}" FontFamily="{StaticResource AppFont}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TelecomProd.Shell;component/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Узел сборки:" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,8,0"/>
            <ComboBox x:Name="UnitCombo" DisplayMemberPath="Name" SelectedValuePath="Id" Width="280" Style="{StaticResource ComboBoxStyle}" SelectionChanged="UnitCombo_SelectionChanged"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Доступные компоненты (двойной клик — добавить в BOM)" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
                    <ListBox x:Name="ComponentsList" Height="320" Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextPrimaryBrush}"
                             MouseDoubleClick="ComponentsList_MouseDoubleClick"/>
                </StackPanel>
            </Border>
            <TextBlock Grid.Column="1" Text="↔" FontSize="24" VerticalAlignment="Center" Foreground="{StaticResource PrimaryBrush}"/>
            <Border Grid.Column="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="Спецификация (BOM)" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
                    <ListBox x:Name="BomList" Height="320" Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextPrimaryBrush}"
                             MouseDoubleClick="BomList_MouseDoubleClick"/>
                </StackPanel>
            </Border>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Сохранить" x:Name="SaveBtn" Click="Save_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
            <Button Content="Закрыть" Click="Close_Click" Style="{StaticResource SecondaryButton}"/>
        </StackPanel>
    </Grid>
</Window>
