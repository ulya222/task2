<Window x:Class="TelecomProd.Shell.Screens.MovementsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="История перемещений" Height="400" Width="600" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}" FontFamily="{StaticResource AppFont}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TelecomProd.Shell;component/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="MovementsGrid" Grid.Row="0" Style="{StaticResource ModernDataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Компонент" Binding="{Binding Component.Name}" Width="*"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding MovementType}" Width="80"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="70"/>
                <DataGridTextColumn Header="Склад" Binding="{Binding Warehouse.Name}" Width="120"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy HH:mm}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="1" Content="Закрыть" Click="Close_Click" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Right" Margin="0,16,0,0"/>
    </Grid>
</Window>
