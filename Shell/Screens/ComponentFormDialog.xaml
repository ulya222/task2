<Window x:Class="TelecomProd.Shell.Screens.ComponentFormDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Компонент" Height="520" Width="520" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}" FontFamily="{StaticResource AppFont}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TelecomProd.Shell;component/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Код (TYPE-XXXXX)" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                <TextBox Text="{Binding Code}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,14"/>
                <TextBlock Text="Наименование" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                <TextBox Text="{Binding Name}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,14"/>
                <TextBlock Text="Тип (active/passive, electronic/mechanical)" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                <TextBox Text="{Binding ComponentType}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,14"/>
                <TextBlock Text="Производитель" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                <TextBox Text="{Binding Manufacturer}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,14"/>
                <TextBlock Text="Мин. запас" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                <TextBox Text="{Binding MinStock}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,14"/>
                <TextBlock Text="Макс. запас" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                <TextBox Text="{Binding MaxStock}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,14"/>
                <TextBlock Text="Поставщик" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                <ComboBox x:Name="SupplierCombo" DisplayMemberPath="Name" SelectedValuePath="Id" Style="{StaticResource ComboBoxStyle}"/>
            </StackPanel>
        </ScrollViewer>
        <Border Grid.Row="1" Padding="0,18,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Применить" Click="Apply_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,14,0"/>
                <Button Content="Отмена" Click="Cancel_Click" Style="{StaticResource SecondaryButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
