<Window x:Class="TelecomProd.Shell.Screens.AuthScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TelecomProd.Shell.ViewModels"
        Title="Вход" Height="520" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="{StaticResource AppFont}">
    <Window.DataContext><vm:AuthViewModel/></Window.DataContext>
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="40,32">
            <StackPanel MaxWidth="340" HorizontalAlignment="Center">
                <TextBlock Text="TelecomProd" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <TextBlock Text="Управление производственным циклом узлов связи" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,28"/>

                <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="28,24" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Логин" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,18"/>

                        <TextBlock Text="Пароль" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{StaticResource SurfaceBrush}">
                            <PasswordBox x:Name="PwdBox" PasswordChanged="PwdBox_PasswordChanged"
                                        Style="{StaticResource PasswordBoxStyle}" BorderThickness="0" Background="Transparent"/>
                        </Border>

                        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource AccentBrush}" TextWrapping="Wrap" Margin="0,14,0,0"
                                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" FontSize="11"/>

                        <Button Content="Войти" Click="SignInButton_Click" Style="{StaticResource PrimaryButton}"
                                HorizontalAlignment="Stretch" Margin="0,22,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
